/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['53.fs1.hubspotusercontent-na1.net'], // Add the domain where your image is hosted
    },
};

export default nextConfig;
